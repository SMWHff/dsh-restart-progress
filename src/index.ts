/**
 * dsh-restart-progress host half. Pure UI plugin: the empty apply exists so
 * the plugin appears in the host cordis tree; the browser half ships via
 * exports["./client"].
 */

/** Host plugin body — no host-side behavior for this surface plugin. */
export function apply(): void {}
